## ---- message=FALSE, warning=FALSE--------------------------------------------
library(teff)

## -----------------------------------------------------------------------------
names(tcell)
head(tcell$teffdata)

## -----------------------------------------------------------------------------
dim(tcell$features)
head(tcell$features)

